import 'package:flutter/material.dart';

class AnalogLogo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(height: 78, child: Image.asset('assets/logo.png'));
  }
}

class AnalogRecieptLogo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(height: 26, child: Image.asset('assets/logo-dark.png'));
  }
}